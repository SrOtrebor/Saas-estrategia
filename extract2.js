const fs = require('fs');
// Start from the ORIGINAL html file content (we must read from Git or I can just use the base64 matches from the original if I didn't overwrite it fully. Wait, I overwrote muestras_diseños.html! Let me use git checkout to restore it first.)
// I will just write a bash script to do this.
